#include <stdio.h>

int main()
{
    // area of rectangle
    // int length, breadth;
    // printf("Enter length\n");
    // scanf("%d", &length);
    // printf("Enter breadth \n");
    // scanf("%d", &breadth);
    // printf("The area of this rectangle is %d", length * breadth);
    // return 0;

    // area of circle

    // float radius;
    // float height;
    // printf("Enter radius of circle \n");
    // scanf("%f", &radius);
    // printf("The area of circle with radius %f is : %f \n", radius, 3.14 * radius * radius);
    // printf("Enter height of cylinder \n");
    // scanf("%f", &height);
    // printf("The volume of cylinder with radius %f and height %f is :%f ", radius, height, 3.14 * radius * radius * height);
    // return 0;

    // celcius to fahrenheit

    // float temp;
    // printf("Enter temperature in celcius %f", temp);
    // scanf("%f", &temp);
    // printf("The temperature in fehrenheit is :%f", 1.8 * temp + 32);
    // return 0;

    // Simple interest

    int p, r, t;
    p = 100000;
    r = 10;
    t = 3;
    printf("The simple interst is %d", (p * r * t) / 100);
    return 0;
}